A new version of Cache is out. Item keys in CacheItem::getKey() no longer contain namespace for apcu, redis and sqlite cache pools.

For complete list of changes since previous version, see CHANGELOG.md.
